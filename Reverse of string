#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    char str[100];
    scanf("%s",str);
    int s=strlen(str);
    for(int i=s-1;i>=0;i--){
        printf("%c",str[i]);
    }
    return 0;
}
